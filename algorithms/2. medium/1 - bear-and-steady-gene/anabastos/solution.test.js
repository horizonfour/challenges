const s = require('./solution')

test('get balance', () => {
  expect(s.getSubstringSize(true)).toEqual(true)
})

test('check if its balanced', () => {
  expect(s.checkIfItsBalanced(true)).toEqual(true)
})

test('get substring', () => {
  expect(s.getSubstring(true)).toEqual(true)
})

test('get substring size', () => {
  expect(s.getSubstringSize(true)).toEqual(true)
})
